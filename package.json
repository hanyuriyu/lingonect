{
  "name": "lingonect-app",
  "version": "1.0.0",
  "private": true,
  "description": "Lingonect iOS app — a Capacitor native wrapper of the Lingonect web app.",
  "type": "module",
  "scripts": {
    "copy-web": "node scripts/copy-web.mjs",
    "sync": "npm run copy-web && cap sync",
    "ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
